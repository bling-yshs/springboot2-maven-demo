server:
  port: 8080

# 配置 Mysql
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/only-user
    username: root
    password: 123456

mybatis:
  mapper-locations: classpath:mapper/*.xml
